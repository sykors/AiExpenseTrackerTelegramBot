# ===================================
# Environment Configuration
# ===================================

# AI Service
GROQAPIKEY=your-groq-api-key-here
GROQ_API_KEY=your-groq-api-key-here

# Telegram Bot
telegramToken=your-telegram-bot-token-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Database Configuration
DATABASE_URL=postgresql://expenseuser:expensepass@db:5432/expensebot
DB_USER=expenseuser
DB_PASSWORD=expensepass
DB_NAME=expensebot

# Redis
REDIS_URL=redis://redis:6379

# Encryption (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Access Control / Demo user
DEFAULT_USER_ID=00000000-0000-0000-0000-000000000001

# Server Configuration (IMPORTANT: Set your domain here!)
# DOMAIN = public hostname for the API (must have DNS A/AAAA record)
DOMAIN=api.yourdomain.com
SERVER_IP=your.server.ip.here

# Web Application / Frontend
# - Lasă WEB_DOMAIN gol sau egal cu DOMAIN pentru scenariul "totul pe același domeniu"
# - Setează WEB_DOMAIN=app.yourdomain.com dacă vrei UI separat
WEB_DOMAIN=app.yourdomain.com
NEXT_PUBLIC_API_URL=https://yourdomain.com/api   # Browser calls (de obicei https://DOMAIN/api)
API_BASE_URL=http://app:8000                     # URL intern folosit de Next.js (nu schimba în Docker)
ADDITIONAL_CORS_ORIGINS=                 # Optional: comma-separated extra origins

# Nginx / SSL behavior
INCLUDE_WWW_VARIANTS=true               # Set to false if you don't want www.<domain> variants

# SSL/HTTPS (for automatic SSL certificate)
SSL_EMAIL=your-email@domain.com

# Application
APP_PORT=8000
LOG_LEVEL=info
